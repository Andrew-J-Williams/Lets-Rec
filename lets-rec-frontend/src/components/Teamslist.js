import React from 'react';

const Teamslist = (props) => {

    const myArray = props.teams
    console.log(myArray)

    return(
        <div>
            Test
        </div>
    )
}

export default Teamslist